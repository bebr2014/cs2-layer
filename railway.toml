[build]
builder = "dockerfile"

[deploy]
startCommand = "python -c \"import psycopg2; conn = psycopg2.connect('postgresql://postgres:bTzcPhuovaxICNnhvFVatINSHxPgPUqs@shinkansen.proxy.rlwy.net:36960/railway', sslmode='require'); print('DB OK'); conn.close()\" && echo 'Running alembic...' && alembic upgrade head && echo 'Alembic done' && python main.py"
restartPolicyType = "on_failure"
